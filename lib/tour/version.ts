/**
 * Current product-tour version. Bump this whenever the walkthrough changes
 * enough that every user should see it again (e.g. a navigation restructure).
 * A user sees the tour while their stored `tour_version` is below this number.
 *
 * History:
 *   1 - Plan hub + Body→Health restructure (2026-06-22).
 */
export const TOUR_VERSION = 1;
